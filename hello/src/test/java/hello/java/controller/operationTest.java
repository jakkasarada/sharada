package hello.java.controller;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.mock;

import org.junit.jupiter.api.Test;

class operationTest {

	addimplements ad = new addimplements();
	@Test
	public void arithetic() {
		ad.addition(2, 2);
		
		
		/*
		 * add a = mock(add.class); int i = 2; int j = 4; operation ope = new
		 * operation(a); int x = ope.addition1(i, j); System.out.println(x);
		 */
	}
}
